import { FeatureDirectiveDirective } from './feature-directive.directive';

describe('FeatureDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FeatureDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
